<!-- <h2><%= t "devise.invitations.new.header" %></h2>

<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post} do |f| %>
  <%= devise_error_messages! %>

<% resource.class.invite_key_fields.each do |field| -%>
  <p><%= f.label field %><br />
  <%= f.text_field field %></p>
<% end -%>

  <p><%= f.submit t("devise.invitations.new.submit_button") %></p>
<% end %> -->
<div class="container page-content album_page">
  <div class="row">
    <div class="row">
      <div class="col m10">
        <%= render "shared/global/return-btn", url: "/users", title: "Retour à la liste des membres" %>
      </div>
    </div>
    <div class="col s11 m11 l11">
      <h1>Nouveau membre</h1>
    </div>
  </div>
  <div class="row">
    <p>Ajouter un nouveau membre pour l'autoriser à accéder aux contenus restreints.</p>
  </div>
  <div class="row">
    <%= render "shared/user/add_user" %>
  </div>
</div>
